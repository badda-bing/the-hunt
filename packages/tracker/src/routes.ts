// packages/tracker/src/routes.ts — no handlers yet. Add to this map
// as tracker's tech stories ship; the wrapper's generate-routes step
// picks them up automatically on next run.

import type { WrappedRoute } from '@baddabing/framework/next'

export const routes: Record<string, WrappedRoute> = {}
